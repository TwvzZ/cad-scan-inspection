# 三个模块测试指令与参数

所有命令在工程根目录执行：

```powershell
cd C:\Users\Administrator\Desktop\cad-scan-inspection
```

## 一、采样模块

入口：`native\sampling\run_sampling_test.ps1`

```powershell
.\native\sampling\run_sampling_test.ps1 [参数]
```

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `-InputStep` | `assets\cad\vg1500040104a.stp` | 输入 STEP 文件 |
| `-OutputPly` | `data\output\sampling\vg1500040104a_sampled_test.ply` | 输出 PLY 文件 |
| `-PointCount` | `200000` | 目标采样点数 |
| `-VoxelSize` | `0.5` | 体素下采样尺寸，毫米；0 表示不使用 |
| `-LinearDeflection` | `0.1` | 曲面三角化线性偏差，毫米 |
| `-AngularDeflection` | `10.0` | 曲面三角化角度偏差，度 |
| `-RelativeDeflection` | `0` | 是否使用相对线性偏差：`0` 关闭，`1` 开启 |
| `-ParallelMeshing` | `1` | 是否启用 OpenCASCADE 并行三角化：`0` 关闭，`1` 开启 |
| `-Seed` | `1` | 随机采样种子；类型 `UInt32`，可取 `0`–`4,294,967,295`，`1` 只是默认值，不是唯一值；相同输入和参数使用相同种子可复现采样结果 |
| `-Mode` | `outer` | `all` 全部、`outer` 外壳、`visible` 可见表面 |
| `-Projection` | `orthographic` | 可见模式投影：`orthographic` 正交、`perspective` 透视 |
| `-ViewX/-ViewY/-ViewZ` | `0,0,-1` | 正交投影视线方向；仅 `Projection orthographic` 生效 |
| `-MaxIncidenceAngle` | `75` | 可见面最大入射角，0.01–89.99 度 |
| `-CameraX/-CameraY/-CameraZ` | `0,0,0` | 透视投影相机位置；仅 `Projection perspective` 生效 |
| `-VisibilityTolerance` | `0` | 可见性射线容差，毫米；`<=0` 使用模型尺寸自动计算 |
| `-VisibilityOversampleFactor` | `4` | 可见模式候选倍率，范围 `1–32`；越大越容易达到目标点数但耗时越长 |

示例：

```powershell
.\native\sampling\run_sampling_test.ps1 `
  -PointCount 600000 `
  -Mode visible `
  -Projection orthographic `
  -ViewX 0 -ViewY 0 -ViewZ -1 `
  -MaxIncidenceAngle 75
```

## 二、配准模块

入口：`native\registration\run_registration_test.ps1`

```powershell
.\native\registration\run_registration_test.ps1 [参数]
```

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `-SourceScan` | `data\input\scan\zhujian_1seg.ply` | 待配准扫描点云 |
| `-TargetCad` | `data\output\sampling\vg1500040104a_sampled.ply` | CAD 参考点云 |
| `-OutputAligned` | `data\output\registration\zhujian_1seg_aligned.ply` | 配准后扫描点云 |
| `-Mode` | `cascade` | `single` 单策略、`cascade` 级联、`ensemble` 集成 |
| `-Strategy` | `all` | `initial` 初始位姿、`pca` PCA、`fpfh` FPFH、`all` 全部 |
| `-MaxDistance` | `5.0` | 最大对应距离，毫米 |
| `-Iterations` | `100` | 迭代次数 |
| `-VoxelSize` | `1.0` | 配准体素尺寸，毫米 |
| `-FeatureVoxelSize` | `5.0` | FPFH 特征体素尺寸，毫米 |
| `-InitialMatrix` | 空 | 4×4 初始变换矩阵文件；空值表示自动估计 |
| `-IcpPreset` | `balanced` | ICP 预设：`fast`、`balanced`、`accuracy` |
| `-RansacMaxIterations` | `50000` | RANSAC 最大迭代次数 |
| `-RansacAttempts` | `4` | RANSAC 尝试次数 |
| `-MaxCandidates` | `4` | 最大候选姿态数 |
| `-MaxRefinedCandidates` | `2` | 最大精配准候选数，不能大于 `MaxCandidates` |
| `-PclRoot` | `third_party\pcl` | PCL 运行库根目录 |

示例：

```powershell
.\native\registration\run_registration_test.ps1 `
  -Mode ensemble `
  -Strategy all `
  -IcpPreset accuracy `
  -RansacMaxIterations 100000 `
  -MaxCandidates 6 `
  -MaxRefinedCandidates 3
```

## 三、检测模块

入口：`native\inspection\run_inspection_test.ps1`

```powershell
.\native\inspection\run_inspection_test.ps1 [参数]
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `-StandardCad` | `data\output\sampling\vg1500040104a_sampled.ply` | CAD 参考点云 |
| `-AlignedScan` | `data\output\registration\zhujian_1seg_aligned.ply` | 配准后的扫描点云 |
| `-OutputDirectory` | `data\output\inspection\run` | 检测结果目录；不存在时自动创建 |

### CadInspectOptions 全部字段

| 分组 | 字段 | 默认值/范围 | 说明 |
|---|---|---|---|
| 坐标系 | `frame.origin` | `[0,0,0]` | ROI 原点 |
| 坐标系 | `frame.axis_u/axis_v` | 单位正交轴 | ROI U/V 方向 |
| 坐标系 | `frame.nominal_normal` | 理论法向 | 理论表面法向 |
| 参考模式 | `reference_mode` | `NARROW_ROI` / `WIDE_MULTIPLANE` | 窄 ROI 或宽区域多平面 |
| 候选平面 | `max_plane_candidates` | 1–8，默认 4 | 最大候选平面数 |
| 候选平面 | `min_candidate_point_ratio` | 0–1，默认 0.03 | 候选平面最小点比例 |
| ROI | `u_min/u_max`、`v_min/v_max` | U/V 范围 | 面内检测区域 |
| 检测厚度 | `detection_normal_min/max` | 默认 -5/20 mm | 缺陷和平面度点的法向范围 |
| 参考厚度 | `reference_normal_min/max` | 默认 -1/0.5 mm | 拟合参考平面的点范围 |
| 边缘 | `edge_margin` | >=0，默认 1 mm | 排除 ROI 边缘点 |
| RANSAC | `ransac_max_iterations` | >0，默认 1000 | 最大迭代次数 |
| RANSAC | `random_seed` | `UInt32`，0–4,294,967,295；默认 1 | 随机种子；1 不是唯一可用值，相同输入和配置使用相同种子可复现结果 |
| RANSAC | `ransac_evaluation_limit` | 0=全部，默认 5000 | 假设评分点数上限 |
| RANSAC | `ransac_confidence` | 0–1，默认 0.999 | 置信度 |
| 平面筛选 | `plane_inlier_distance` | >0，默认 0.15 mm | 点到平面的内点阈值 |
| 平面筛选 | `max_normal_angle_deg` | 0–90，默认 5° | 最大法向夹角 |
| 平面筛选 | `min_reference_points` | >=3，默认 100 | 最少参考点数 |
| 平面筛选 | `min_reference_inlier_ratio` | 0–1，默认 0.5 | 最小内点比例 |
| 覆盖率 | `coverage_grid_u/v` | >0，默认 10/10 | 覆盖率网格尺寸 |
| 覆盖率 | `min_reference_grid_coverage` | 0–1，默认 0.2 | 最小覆盖率 |
| 覆盖率 | `max_reference_rmse` | >0，默认 0.1 mm | 最大拟合 RMSE |
| 缺陷 | `positive_defect_threshold` | >0，默认 0.3 mm | 凸起阈值 |
| 缺陷 | `negative_defect_threshold` | >0，默认 0.3 mm | 凹陷阈值 |
| 缺陷 | `defect_cluster_cell_size` | >0，默认 0.5 mm | 聚类网格尺寸 |
| 缺陷 | `min_defect_points` | >0，默认 5 | 最小缺陷点数 |
| 缺陷 | `min_defect_area` | 默认 0.25 mm² | 最小缺陷面积 |
| 缺陷 | `max_output_defects` | >0，默认 256 | 最大输出缺陷数 |
| 平面度 | `flatness_trim_fraction` | 0–0.5，默认 0.001 | 鲁棒平面度两端截尾比例 |
| 平面度 | `flatness_working_grid_size` | <=0=全部，默认 0.5 mm | 最小区域法工作网格 |
| 平面度 | `minimum_zone_max_iterations` | >0，默认 100 | 最小区域法最大迭代次数 |
| 平面度 | `minimum_zone_tolerance` | >0，默认 1e-7 | 最小区域法收敛容差 |

注意：检测参数不是 PowerShell 参数，目前在 `native\inspection\examples\ply_inspection_test.cpp` 中的 `Configure`/`CadInspectOptions` 初始化处修改，修改后重新编译 inspection。

### 检测模块的两个 DLL

检测工程会同时生成两个 ABI 相同的 DLL：

| DLL | 默认 `reference_mode` | 适用场景 | 对应测试程序 |
|---|---|---|---|
| `cad_inspection.dll` | `NARROW_ROI` | 检测面附近高度范围较窄、参考平面明确的常规检测；生产默认使用它 | `cadinspect_ply_test.exe`、`cadinspect_test.exe` |
| `cad_inspection_wide.dll` | `WIDE_MULTIPLANE` | ROI 较宽、包含多个平面或需要从宽区域自动筛选参考平面的场景 | `cadinspect_wide_test.exe` |

两者导出函数、结构体和参数完全一致，区别只在编译时默认参考模式；调用方仍可通过 `CadInspectOptions.reference_mode` 显式覆盖默认值。因此：窄 ROI 选普通 DLL，宽 ROI/多平面选 wide DLL，不要在同一进程同时加载两个同名接口版本。PowerShell 的 `run_inspection_test.ps1` 当前调用普通 DLL 链接的 `cadinspect_ply_test.exe`；要测试 wide 版本，应运行对应的 `cadinspect_wide_test.exe`，或在 C++ 调用中链接 `cad_inspection_wide`。

```powershell
cmake --build build\inspection --config Release
```

## 参数来源与完整性说明

- PowerShell 脚本参数以三个入口文件的 `param(...)` 为准；`ValidateSet` 是枚举可选值，数值类型和 `ValidateRange` 是脚本层约束。
- 检测模块的全部算法参数是 `CadInspectOptions` 字段，不是检测脚本命令行参数；修改 `native\inspection\examples\ply_inspection_test.cpp` 后重新编译 DLL/测试程序。
- ABI 字段 `struct_size`、`abi_version` 用于兼容性校验，不属于算法调参项；`frame` 下的原点、U/V 轴和理论法向共同定义 ROI 坐标系。
- 运行时还会执行 C++ 层范围校验；脚本能接受的值不代表一定适合数据，尤其是点数、体素尺寸、阈值和候选数量会受到模型尺寸、内存和运行时间限制。
- 如需查看当前源码中的完整声明：

  ```powershell
  Get-Content native\sampling\run_sampling_test.ps1
  Get-Content native\registration\run_registration_test.ps1
  rg -n "CadInspectOptions|random_seed|Configure" native\inspection
  ```

## 四、完整流程

```powershell
.\native\sampling\run_sampling_test.ps1 -PointCount 600000
.\native\registration\run_registration_test.ps1 -Mode cascade -Strategy all
.\native\inspection\run_inspection_test.ps1
```

## 五、输出和返回码

- `0`：执行成功。
- `1`：执行完成，但检测未通过或存在检测面失败。
- `2`：输入、参数或运行库错误。

检测输出包括 `inspection_result.txt`、彩色结果 PLY、检测 ROI、参考 ROI 和拟合平面 PLY。
